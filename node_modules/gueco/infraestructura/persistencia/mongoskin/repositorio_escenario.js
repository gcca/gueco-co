/*! \package infraestructura.persistencia.mongo
 */

var repositorio = require(
    'gueco/infraestructura/persistencia/mongoskin/repositorio')

repositorio_escenario.prototype = new repositorio
repositorio_escenario.prototype.constructor =
    repositorio_escenario

repositorio_escenario.prototype.coleccion = 'escenario'

function repositorio_escenario () {}

module.exports = repositorio_escenario
