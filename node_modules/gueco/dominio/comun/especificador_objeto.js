/*! \package dominio.comun
 */

var especificador = require('gueco/dominio/comun/especificador')

especificador_objeto.prototype = new especificador
especificador_objeto.prototype.constructor = especificador_objeto

especificador_objeto.prototype.especificar = function(t) {
    return this.objeto;
}

function especificador_objeto (objeto) { especificador.call(this, objeto) }

module.exports = especificador_objeto
